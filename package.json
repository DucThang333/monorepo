{
  "name": "IZONE",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "pnpm --filter @package/cli run generate",
    "format": "pnpm prettierrc",
    "storybook:ui": "pnpm --filter @package/ui run storybook",
    "dev:lms": "pnpm --filter @lms run dev"
  },
  "license": "MIT",
  "private": true
}
